# VerifDoc - Dependencies (pinned for reproducibility)
# Core
fastapi==0.115.0
uvicorn==0.30.1
python-multipart==0.0.9

# Image processing & forensics
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
PyWavelets==1.6.0

# PDF handling
PyMuPDF==1.24.9

# OCR (optional - for scanned images only, PyMuPDF handles PDF text)
pytesseract==0.3.13

# Dashboard
streamlit==1.38.0

# AI — Claude Vision (optional, set ANTHROPIC_API_KEY to enable)
anthropic>=0.39.0

# Utilities
python-dateutil==2.9.0
